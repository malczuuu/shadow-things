server:
  port: 8080

spring:
  application:
    name: shadow-things
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: shadow-things
  jackson:
    default-property-inclusion: non_null
  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: user
